import os
from dotenv import load_dotenv

# Загружаем переменные окружения
load_dotenv()

# Токен бота
BOT_TOKEN = os.getenv('BOT_TOKEN', '8179418366:AAE3LVLivo0DJu-_-m41OsB9RyKtYV5if9Y')

# ID администраторов (преобразуем строку в список чисел)
ADMIN_IDS_STR = os.getenv('ADMIN_IDS', '7479394466, 5063450800')
ADMIN_IDS = [int(id.strip()) for id in ADMIN_IDS_STR.split(',') if id.strip()]

# Название базы данных
DB_NAME = os.getenv('DB_NAME', 'tyumenchat.db')

# Режим отладки
DEBUG = os.getenv('DEBUG', 'False').lower() == 'true'

# Районы Тюмени
TYUMEN_DISTRICTS = [
    "🏛️ Центральный",
    "🏭 Калининский", 
    "🏘️ Ленинский",
    "🌳 Восточный",
    "🏞️ Мыс",
    "🏡 Тарманы",
    "🏕️ Комарово",
    "🌲 Гилевская роща",
    "🏘️ МЖК",
    "🏛️ Зарека",
    "🏭 Нефтяников",
    "🌿 Дружба"
]

# Проверка конфигурации
if not BOT_TOKEN:
    raise ValueError("BOT_TOKEN не установлен в .env файле!")

if not ADMIN_IDS:
    raise ValueError("ADMIN_IDS не установлены в .env файле!")

print(f"✅ Конфигурация загружена:")
print(f"   Бот токен: {BOT_TOKEN[:10]}...")
print(f"   Админы: {ADMIN_IDS}")
print(f"   База данных: {DB_NAME}")